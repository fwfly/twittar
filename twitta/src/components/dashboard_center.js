import React from 'react';
import StreamList from "../containers/stream_list";

export default ()=>{
  return(
    <div className="dashboard-center">
      tweet box
      stream item
      <StreamList />
    </div>
  );
}
