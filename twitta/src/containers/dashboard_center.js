import React from 'react';
import StreamList from '../components/stream_list';

export default ()=>{
  return(
    <div className="dashboard-cneter">
      dashboard center
      <StreamList />
    </div>
  );
}
